# Basic Auth credentials for the pre-launch password gate.
# Set these in your hosting provider (Vercel → Settings → Environment Variables)
# and/or locally in a `.env.local` file (never commit that file).
#
# If both are unset, the site is open (useful for local development).
BASIC_AUTH_USER=alex
BASIC_AUTH_PASSWORD=change-me-to-something-strong
